dist
coverage
node_modules
.repos
repos
pnpm-lock.yaml
docs/csp
docs/CSP_STATUS.md

# Ported modernization data: pack manifests carry regexes whose exact text is
# load-bearing, pattern cards embed COBOL fragments, and the scaffold is a
# target-project template — none of it is llm4ts source to restyle.
flows/fixtures
kits/*/packs
kits/*/patterns
kits/*/scaffolds
kits/*/fixtures
packages/shell/flows
packages/shell/kits

# Internet-banking portal fixture: a standalone Vite/React toolchain with its
# own style, seeded into a working copy by the epic-stories runbook.
examples/internet-banking/portal
